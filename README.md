# Mcpanel

hopefully a webapp to automatically set up a minecraft server

assumes running on a unix machine with podman installed

# Features (maybe)
- create a server
- delete a server
- install plugins automatically
- install datapacks automatically
- restart a server
- rcon
